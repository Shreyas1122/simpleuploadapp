<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Media Display Portal</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f8f9fa;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
        }
        .container {
            display: flex;
            flex-direction: column;
            gap: 20px;
            padding: 20px;
            max-width: 900px;
            margin: auto;
        }
        .card {
            background: white;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            text-align: center;
            padding: 15px;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 15px;
        }
        .media-container {
            display: flex;
            flex-direction: row;
            gap: 10px;
            justify-content: center;
        }
        .media {
            width: 300px;
            height: 250px;
        }
        iframe {
            width: 250px;
            height: 250px;
        }
        .card-content {
            width: 100%;
            text-align: center;
        }
        @media (max-width: 768px) {
            .card {
                flex-direction: column;
            }
            .media-container {
                flex-direction: column;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <% arrays.forEach((entry) => { %>
            <div class="card">
                <div class="media-container">
                    <img class="media" src="<%= entry.imageurl %>" alt="Image">
                    <iframe class="media" src="<%= entry.pdfurl %>" frameborder="0"></iframe>
                </div>
                <div class="card-content">
                    <h3><%= entry.title %></h3>
                    <p><%= entry.descrpt %></p>
                </div>
            </div>
        <% }); %>
    </div>
</body>
</html>
